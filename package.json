{
  "name": "component",
  "version": "1.0.0",
  "description": "A composable component inspired by React.",
  "main": "events.js",
  "scripts": {
    "test": "browserify --bare test.js | tape-run --node"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.2",
    "tape": "^4.9.1",
    "tape-run": "^4.0.0"
  },
  "dependencies": {
    "he": "^1.1.1"
  }
}
